<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.front" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.ClassLogger"
        tools:targetApi="31">
        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Register Activity -->
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false" />

        <!-- Teacher Activities -->
        <activity
            android:name=".ui.teacher.TeacherDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.teacher.TeacherCreateSessionActivity"
            android:exported="false" />
        <activity
            android:name=".ui.teacher.TeacherViewAttendanceActivity"
            android:exported="false" />
        <activity
            android:name=".ui.teacher.TeacherActiveSessionsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.teacher.SessionAttendanceListActivity"
            android:exported="false" />

        <!-- Student Activities -->
        <activity
            android:name=".ui.student.StudentDashboardActivity"
            android:exported="false" />
        <activity
            android:name=".ui.student.StudentActiveSessionsActivity"
            android:exported="false" />
        <activity
            android:name=".ui.student.StudentAttendanceActivity"
            android:exported="false" />
        <activity
            android:name=".ui.student.StudentAttendanceHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".ui.student.StudentSubjectDetailActivity"
            android:label="Subject Details"
            android:parentActivityName=".ui.student.StudentAttendanceHistoryActivity"/>
    </application>

</manifest>